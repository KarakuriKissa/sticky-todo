node_modules/
dist/
src-tauri/target/
src-tauri/gen/
# Generated icon variants are not committed, but the SOURCE png IS committed
# so the macOS CI job (which can't run the Windows GDI+ generator) can reuse it.
src-tauri/icons/
!src-tauri/icons/icon-source.png
*.log
.DS_Store
Thumbs.db

# Build / CI logs downloaded for debugging
logs/

# Local environment files
.env
.env.local

# Coverage reports
coverage/

src-tauri/icons_v1_backup/

worker/node_modules/
worker/.wrangler/
